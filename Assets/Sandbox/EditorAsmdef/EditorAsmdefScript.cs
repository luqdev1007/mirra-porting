namespace Sandbox.EditorAsmdef
{
    /// <summary>
    /// Covered by an asmdef limited to the Editor platform, so it compiles into an editor
    /// assembly although nothing in the path says "Editor". Expected EditorOnly: true.
    /// </summary>
    public static class EditorAsmdefScript
    {
    }
}
