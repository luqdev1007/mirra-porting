namespace Sandbox.Plain
{
    /// <summary>
    /// Ordinary game code: no asmdef, no Editor folder, so it compiles into Assembly-CSharp.
    /// Expected EditorOnly: false.
    /// </summary>
    public static class PlainScript
    {
    }
}
